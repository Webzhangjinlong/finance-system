# CI 门禁（Gate 4）
# 触发：push main / PR 到 main
# 作用：build + 测试 + ArchUnit 架构守护 + Flyway 迁移校验 + 覆盖率报告
# 门禁：main 分支保护 required status checks = backend-ci, frontend-ci
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-ci:
    name: backend-ci
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: finance-system-test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Backend verify (test + ArchUnit + JaCoCo + Flyway)
        working-directory: finance-server
        env:
          TEST_DB_HOST: localhost
          TEST_DB_PORT: 5432
          TEST_DB_NAME: finance-system-test
          TEST_DB_USER: postgres
          TEST_DB_PASSWORD: postgres
        run: mvn -B -pl finance-admin -am verify

      - name: Upload surefire reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-reports
          path: finance-server/**/target/surefire-reports/

      - name: Upload jacoco report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: finance-server/**/target/site/jacoco/

  frontend-ci:
    name: frontend-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: finance-web/package-lock.json

      - name: Install & build
        working-directory: finance-web
        run: |
          npm ci
          npm run build
